Counter-WAF mutations from a blocked payload (not blind spawn-after-bypass only).

mutate_from_blocked(payload) -> [(payload, label), ...]

Used by evasion/run_until_bypass.py with --adv-from-block.

Imports evasion.transformers — run scripts from ml-waf-experiment root so "evasion" resolves.
