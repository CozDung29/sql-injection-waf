\section{Experiments and Results}
\label{sec:experiments}
We answered RQ1--RQ3 using two datasets exported from our repository. The main dataset came from live AWS WAF probing, and the secondary dataset came from the RFC decoding surrogate pilot.

\Needspace{10\baselineskip}
\subsection{Experiment Setup and Environment}
We ran all scripts on a Windows machine with version 10.0.26200 and 15.84 GB RAM. The experiment code is under \texttt{paper/experiment}, mainly \texttt{ml-waf-aws-test}, \texttt{ml-waf-blocked-adv}, \texttt{ml-waf-multi-eval}, and \texttt{ml-waf-adversarial-eval}. The Python dependencies used in these folders include \texttt{requests>=2.28.0}, \texttt{numpy>=1.24.0}, \texttt{scikit-learn>=1.3.0}, and \texttt{joblib>=1.3.0}. For the live run, we used \texttt{run\_probe.py} and saved outputs to JSONL/TXT/JSON files under each module's \texttt{data} folder.

\Needspace{12\baselineskip}
\subsection{Attack Scenarios and What We Observed}
We followed the same flow every time so the runs were comparable:
\begin{enumerate}
  \item We started with SQLi seeds from PostgreSQL/Oracle style payload sets, then remapped IDs to the target parameter format.
  \item We enabled mutation rules and optional second-layer transforms, then sent GET requests to the WAF-protected endpoint.
  \item We logged each HTTP response as \textsf{bypass}, \textsf{blocked}, or \textsf{other} based on the classifier logic in \texttt{run\_probe.py}.
  \item We repeated this process at larger request counts and compared family-level behavior from exported summaries.
\end{enumerate}
During probing, we observed two stable patterns: identity/case/null-byte families often passed at high rates, while encoding-heavy families were mostly blocked in this deployment.

\Needspace{12\baselineskip}
\subsection{Measurement Method}
We measured three groups of outputs:
\begin{itemize}
  \item \textbf{Attempt-level counts:} total requests, bypass count, and blocked count from \texttt{aws\_probe\_summary.json} and JSONL logs.
  \item \textbf{Family-level rates:} $N_f$ and $\mathrm{BR}_f$ from exported tables in \texttt{paper-attack-evasion}.
  \item \textbf{Surrogate pilot accuracy:} test accuracy from \texttt{defense\_eval.json} before and after \texttt{unquote\_chain}.
\end{itemize}
Data collection was file-based. Every request appended one JSON line, then the reporting scripts aggregated those lines into summary JSON/CSV/TXT files. We kept all metrics exactly as exported and did not manually edit numeric outputs.

\Needspace{10\baselineskip}
\subsection{RQ1: Aggregate Scale, Labels, and Unique Payloads}
The archived aggregate contains $N=9{,}435$ HTTP attempts with $N_{\mathrm{bp}}=3{,}721$ bypass-labeled outcomes, so $\mathrm{BR}_{\mathrm{agg}}=0.394$ (39.4\%). The mutation report for the same environment lists 3{,}724 bypass records and 5{,}687 blocked records. Unique bypass payloads are 42 and unique blocked payloads are 147, with 24 payloads appearing in both sets at least once.

This gap between attempts and unique payload strings means the search revisited similar strings under different transform labels or generations. The overlap between bypass and blocked sets also shows that payload text alone did not always decide the final outcome. Table~\ref{tab:dataset} shows the corpus summary.

\begin{table*}[t]
  \centering
  \caption{AWS WAF lab attack corpus (exported aggregate).}
  \label{tab:dataset}
  \begin{tabular}{lrr}
    \toprule
    Metric & Value & Notes \\
    \midrule
    HTTP attempts $N$ & 9{,}435 & Exported probe summary \\
    Bypass count $N_{\mathrm{bp}}$ & 3{,}721 & $\mathrm{BR}_{\mathrm{agg}}=39.4\%$ \\
    Unique bypass payloads & 42 & Mutation report \\
    Unique blocked payloads & 147 & Mutation report \\
    Payloads in both sets & 24 & Non-disjoint label sets \\
    \bottomrule
  \end{tabular}
\end{table*}

\Needspace{10\baselineskip}
\subsection{RQ1 (continued): Channel and Workload Shape}
Our injection path used URL-visible slots with remapped SQLi fragments. The workload exercised percent-encoding changes, case changes, delimiter tricks, and dialect-specific spellings. This archived run does not directly test multipart \texttt{form-data}, JSON bodies, or XML bodies, so those channels need extra probes. Also, this dataset is attack-heavy by design, so it is not a benign baseline for false-positive analysis.

\Needspace{10\baselineskip}
\subsection{RQ2: Family-Level Heterogeneity}
Figure~\ref{fig:bypass} highlights the qualitative structure: aggregate bypass is substantial, but the mass is not spread evenly across operators.

\begin{figure}[t]
  \centering
  \fbox{\parbox{\columnwidth}{\centering $\mathrm{BR}_{\mathrm{agg}}\approx 0.39$ over $N{=}9{,}435$; spawn\_encoding cohort mostly blocked; identity/case/null-byte spawns high $\mathrm{BR}_f$}}
  \caption{Archived live-probe summary: aggregate bypass rate and contrasting family-level behavior (Table~\ref{tab:bypass_categories}).}
  \label{fig:bypass}
\end{figure}

Table~\ref{tab:bypass_categories} lists representative families with large $N_f$. The \emph{spawn\_identity}, \emph{spawn\_case\_mix}, and \emph{spawn\_null\_byte} cohorts stay around $\mathrm{BR}_f=0.89$--$0.90$. In contrast, \emph{spawn\_encoding} is around 0.025 and mostly blocked. In this deployment, that pattern matches a WAF policy that handled nested encodings more aggressively than case or null-byte suffix mutations. Hex/comment-oriented families often had $\mathrm{BR}_f=0$ at the attempted volumes in this export.

\textbf{Statistical caution.} Per-family rates are binomial proportions conditioned on the observed $N_f$ for family $f$. Families with very small $N_f$ (for example, two attempts) can show $\mathrm{BR}_f=1$ but still be unreliable. The exported analysis pipeline includes confidence intervals in CSV/JSON outputs, so those files should be checked when comparing families with very different attempt counts.

\begin{table*}[t]
  \centering
  \caption{Selected mutation families (AWS WAF lab export): attempts $N_f$ and approximate $\mathrm{BR}_f{=}N_{f,\mathrm{bp}}/N_f$.}
  \label{tab:bypass_categories}
  \begin{tabular}{lrrr}
    \toprule
    Family & $N_f$ & $\mathrm{BR}_f$ (\%) & Role \\
    \midrule
    spawn\_identity & 1{,}808 & $\approx$89.1 & High yield \\
    spawn\_case\_mix & 459 & $\approx$89.8 & High yield \\
    spawn\_null\_byte & 1{,}807 & $\approx$89.0 & High yield \\
    spawn\_encoding & 3{,}471 & $\approx$2.5 & Mostly blocked \\
    hex\_account\_style & 2 & $\approx$100.0 & Tiny $N_f$ \\
    \bottomrule
  \end{tabular}
\end{table*}

\Needspace{10\baselineskip}
\subsection{RQ3: Surrogate Accuracy Under RFC-Style Decoding}
Table~\ref{tab:bypass_rates} summarizes the offline pilot. The random-forest test accuracy stayed at 42.9\% for both raw strings and \texttt{unquote\_chain}-canonicalized strings on the 20-request split in \texttt{defense\_eval.json}. In this small sample, fixpoint percent-decoding did not improve or reduce surrogate performance.

\begin{table*}[t]
  \centering
  \caption{Offline surrogate pilot ($n{=}20$ labeled requests): test accuracy, raw vs.\ RFC-style canonicalized strings.}
  \label{tab:bypass_rates}
  \begin{tabular}{lcc}
    \toprule
    Condition & Test accuracy & Notes \\
    \midrule
    Raw strings & 0.429 & Same seed and split \\
    After \texttt{unquote\_chain} & 0.429 & Fixpoint \texttt{unquote\_plus} \\
    \bottomrule
  \end{tabular}
\end{table*}

\Needspace{10\baselineskip}
\subsection{Threats to Validity and Confounds}
\textbf{Label validity.} Bypass labels encode a script-specific mapping from HTTP observables to outcomes; different applications would require recalibration.

\textbf{Policy snapshot.} AWS WAF rules, rate limits, and IP reputation evolve; our numbers describe one archived run.

\textbf{Search bias.} Queue order, caps, and learned transforms skew which families receive high $N_f$; $\mathrm{BR}_f$ compares families under that skewed design rather than under uniform random mutation.

\textbf{Surrogate mismatch.} The random forest is not a proxy for the managed WAF; RQ3 addresses only a narrow normalization hypothesis.

\Needspace{10\baselineskip}
\subsection{Measurement Overhead and Ethics}
The harness prioritizes logging fidelity over maximum throughput, and sleep intervals plus request caps are configurable. All experiments were designed for \emph{authorized} targets only; probing without authorization is illegal and unethical.

\Needspace{10\baselineskip}
\subsection{Reproducibility}
Summaries can be regenerated with scripts under \texttt{ml-waf-aws-test/paper-attack-evasion} and \texttt{ml-waf-aws-test/paper-defense-rfc-ml}. Some JSON metadata may include absolute paths from the original workstation, so those paths should be updated after checkout. Running \texttt{run\_probe.py} against a live endpoint will produce fresh logs, but counts will only match archived values if both configuration and WAF policy are the same.
